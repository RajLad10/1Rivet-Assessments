import { StatusColourPipe } from './status-colour.pipe';

describe('StatusColourPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusColourPipe();
    expect(pipe).toBeTruthy();
  });
});
